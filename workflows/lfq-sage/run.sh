#!/usr/bin/env bash
set -euo pipefail

# shellcheck source=/dev/null
source "$(dirname "$0")/../lib/search-sage.sh"   # defines run_search()
# shellcheck source=/dev/null
source "$(dirname "$0")/../common.sh"
