data/cache/
config.toml
*.worktree/
.pytest_cache/
__pycache__/
results/results.tsv
# Anchored to repo root: the vendored checkout only. Unanchored "OpenMS/" also
# matched scripts/openms/ on case-insensitive filesystems (Windows), silently
# un-tracking the run scripts.
/OpenMS/
results/
.venv/
.venv-test/
